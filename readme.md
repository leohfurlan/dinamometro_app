# Dinamômetro Serial (Modbus RTU)

Este projeto permite ler em tempo real os dados de força de um dinamômetro via protocolo Modbus RTU (RS-232 ou RS-485).

## ⚙️ Requisitos

```bash
pip install -r requirements.txt
